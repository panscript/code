/*

A constant is a variable whose value cannot change.

Constants are defined as:
constant type name = value

*/

constant text name = "John"
constant number euler = exponential(1)

write(name)
write(euler)


// Trying to reassign the value of a constant
// causes an error before the code starts to run

name = "Peter"  // comment out this line to fix the error